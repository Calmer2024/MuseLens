### A1: 视觉解析层 (Vision Parser)

*不产生新画面，专门负责提取物理特征与身份信息，作为下游生成引擎的“约束锚点”。*

| Lens ID               | 名称         | 核心功能                                           | Inputs 资产 | Outputs 资产   | Params (向外暴露参数) | 微调 UI 控件 (前端交互映射)                                  | 底层节点技术         |
| --------------------- | ------------ | -------------------------------------------------- | ----------- | -------------- | --------------------- | ------------------------------------------------------------ | -------------------- |
| lens_mask_core        | 核心遮罩提取 | 语义抠图后向内腐蚀，提取物体最中心的绝对保护区。   | BASE_IMAGE  | CORE_MASK      | prompt (识别目标)     | 智能套索与涂抹画笔：允许用户在 AI 识别的基础上，手动补充或擦除遮罩区域，后端实时更新输出。 | SAM2 / BiRefNet      |
| lens_depth_extract    | 深度提取     | 提取画面的 3D 深度图，作为下游空间约束。           | BASE_IMAGE  | DEPTH_MAP      | 无                    | 结构清晰度滑块：前端滑动，后端映射到不同的提取分辨率或预处理器强度，让结构线变粗或变细。 | DepthAnythingV2      |
| lens_canny_extract    | 边缘线稿提取 | 提取画面的边缘线稿，作为下游空间约束。             | BASE_IMAGE  | CANNY_MAP      | 无                    |                                                              | Canny                |
| lens_pose_extract     | 姿态骨骼提取 | 提取人物关节点数据，用于严格锁定重绘时的主体动作。 | BASE_IMAGE  | POSE_MAP       | 无                    | 2D 骨骼关节拖拽画布：渲染出火柴人，允许用户拖拽关节（如抬高手臂），将修改后的图发给后端。 | DWPose / OpenPose    |
| lens_identity_extract | 身份特征提取 | 提取原图主体（人脸/衣服）的高维向量特征。          | BASE_IMAGE  | IDENTITY_EMBED | 无                    | 身份锚点开关：提供“锁脸”、“锁衣服”的 Toggle 按钮组，供后端决定是否串联对应的 IP-Adapter 节点。 | CLIP Vision / FaceID |

### A2: 像素修改与语义重构层 (Modifier & Reconstructor)

*系统的心脏。全面升级为 FLUX.2 Klein 4B 引擎，通过原生的* *`ReferenceLatent`* *与* *`SamplerCustomAdvanced`* *潜空间操作，执行极度精准的文本意图与图像编辑。*

| Lens ID                 | 名称           | 核心功能                                                     | Inputs 资产                          | Outputs 资产 | Params (实际对应 JSON 节点参数)               | 微调 UI 控件 (前端交互映射)                                  | 底层节点技术                |
| ----------------------- | -------------- | ------------------------------------------------------------ | ------------------------------------ | ------------ | --------------------------------------------- | ------------------------------------------------------------ | --------------------------- |
| lens_flux_text2image    | 基础文本生图   | 纯文字驱动的画面生成起点。无中生有地构建高质量像素画面。     | 无                                   | RESULT_IMAGE | prompt, width, height, steps, cfg, noise_seed | 魔法灵感骰子 & 比例切片：骰子点击映射到随机 noise_seed 和动态 prompt 补充；切片选框直接映射 width/height。 | FLUX.2 + EmptyLatent        |
| lens_flux_edit          | 无约束语义重绘 | 无遮罩。仅通过提示词全局重塑风格、光影或材质，通过潜空间参考原图保持构图。 | BASE_IMAGE                           | RESULT_IMAGE | prompt, steps, cfg, noise_seed    | 创意发散度引擎 (仪表盘)：由于没有 denoise，前端仪表盘高低直接映射后端的 steps 和 cfg。发散度越高，cfg 适当提升，让 prompt 覆盖原图特征。 | FLUX.2 + ReferenceLatent    |
| lens_flux_reference     | 单约束参考重绘 | 引入 1 张 A1 层资产（如深度图），在重绘时利用参考潜空间强制锁定画面的物理特征。 | BASE_IMAGE, REF_IMAGE_1              | RESULT_IMAGE | prompt, steps, cfg, noise_seed                | 特征保留天平 (Balance Slider)：既然底层没有 ref_weight，左滑“偏向文本”，右滑“偏向参考图”。后端将其翻译为 Prompt 语法权重（如 (keyword:1.5)）以对抗参考潜空间。 | FLUX.2 + ReferenceLatent x2 |
| lens_flux_two_reference | 双约束参考重绘 | 引入 2 张 A1 层资产，在重绘时对画面进行多维度死锁保护（如既锁深度又锁边缘）。 | BASE_IMAGE, REF_IMAGE_1, REF_IMAGE_2 | RESULT_IMAGE | prompt, steps, cfg, noise_seed                | 约束混音器 (DJ Mixer)：提供多条推子（形体推子、深度推子）。推子高低控制后端 Prompt 中关于该约束的负面提示词抑制强度，或动态决定是否 bypass 某条参考流。 | FLUX.2 + ReferenceLatent x4 |
| lens_flux_inpaint       | 局部遮罩重绘   | 接收 A1 遮罩，通过 SetLatentNoiseMask 仅对白色区域进行重绘，完美融合环境光。 | BASE_IMAGE, MASK                     | RESULT_IMAGE | prompt,  steps, cfg, noise_seed               | 魔法局部微光球 (3D Light Orb)：用户拖拽光球到遮罩附近。光球方位和亮度被后端提取，直接转化为注入到 prompt 里的强光影提示词，精确控制该局部的反光方向。 | FLUX.2 + SetLatentNoiseMask |

### A3: 光影与物理渲染层 (Relighting Engine & Optical Simulation)

*解决物理合成的后遗症，赋予画面真实世界的光学属性。本层分为两种策略：使用 FLUX.2 潜空间参考进行全局光影重构，或使用纯物理光学节点进行镜头级渲染。*

| Lens ID             | 名称             | 核心功能                                                     | Inputs 资产           | Outputs 资产 | Params (实际对应 JSON 节点参数)                              | 微调 UI 控件 (前端交互映射)                                  | 底层节点技术                                       |
| ------------------- | ---------------- | ------------------------------------------------------------ | --------------------- | ------------ | ------------------------------------------------------------ | ------------------------------------------------------------ | -------------------------------------------------- |
| lens_relighting     | 全局光影重构透镜 | 结合原图与深度图。通过 FLUX 潜空间参考机制，在绝对保护物理体积（深度）的前提下，根据文字指令重写全局光照方向、环境光遮蔽与色温。 | BASE_IMAGE, DEPTH_MAP | RESULT_IMAGE | prompt (光影指令), steps (去噪步数), cfg (引导系数), noise_seed (随机种) | 3D 悬浮光球 (3D Light Orb)：用户在前端拖拽光球位置、调节光强与色温。后端将空间坐标(x,y,z)精准翻译为摄影级 Prompt（如“右上方强烈逆光，暖金色色温”），并配合微调 steps 来控制光影的覆盖深度。 | FLUX.2 Klein 4B + ReferenceLatent                  |
| lens_depth_of_field | 景深与焦距透镜   | 纯光学模拟。依据深度图信息，模拟真实单反镜头的焦平面、物理散景(Bokeh)、色散与大气雾霾，秒级出图，拒绝 AI 塑料感。 | BASE_IMAGE, DEPTH_MAP | RESULT_IMAGE | dof_focus_point (对焦平面), dof_intensity (虚化强度), dof_sharpness_radius (清晰范围), vignette_intensity (暗角强度), halation_strength (光晕强度) | 触控对焦 & 光圈转盘 (Tap-to-Focus & Aperture)：用户点选画面任意位置（如人眼），后端瞬间读取该坐标在深度图中的灰度值并赋值给 dof_focus_point (0.0-1.0)；前端的光圈环拨动，则直接映射后端的 dof_intensity 与 dof_sharpness_radius 参数组合。 | Optical Realism & Physics (纯光学卷积，无扩散模型) |

### A4:风格化与色彩映射层

| Lens ID          | 名称          | 核心功能                                                     | Inputs 资产                       | Outputs 资产 | Params (实际对应 JSON 节点参数)                              | 微调 UI 控件 (前端交互映射)                                  | 底层节点技术                                       |
| ---------------- | ------------- | ------------------------------------------------------------ | --------------------------------- | ------------ | ------------------------------------------------------------ | ------------------------------------------------------------ | -------------------------------------------------- |
| lens_style       | 风格迁移透镜  | 结合原图结构与参考图的风格特征。通过 IPAdapter 提取参考图的风格与语义，结合 Depth 深度图约束，在保持原图物理结构的前提下，将画面重绘为全新的艺术风格。 | BASE_IMAGE, STYLE_REFERENCE_IMAGE | RESULT_IMAGE | prompt (正向文本提示), ipadapter_weight (风格权重), controlnet_strength (深度控制强度), denoise (降噪重绘幅度), steps (迭代步数), seed (随机种子) | 风格参考卡片 (Style Reference)：用户上传或选择风格图。风格强度滑块 (Style Intensity)：前端滑块映射至后端的 ipadapter_weight (如 0.0-1.0)；结构保留滑块 (Structure Preservation)：映射至后端的 controlnet_strength 与 ksampler 的 denoise，控制风格化对原图的颠覆程度。 | SDXL + IPAdapter Plus + DepthAnythingV2 ControlNet |
| lens_lora_filter | LoRA 滤镜透镜 | 为画面叠加特定的艺术滤镜效果（如复古水彩）。基于深度图 ControlNet 锁定画面主体结构，仅通过 LoRA 权重改变画面材质、笔触、色彩和氛围，拒绝破坏基础构图。 | BASE_IMAGE                        | RESULT_IMAGE | lora_name (滤镜模型名称), strength_model & strength_clip (滤镜模型强度), prompt (滤镜专属提示词), denoise (重绘幅度), steps (迭代步数), seed (随机种子) | 滤镜选择器 (Filter Selector)：用户在前端选择滤镜，映射至后端的 lora_name 及预设 prompt；滤镜浓度滑块 (Filter Opacity)：前端滑块 (0.0-1.0) 动态同步控制后端的 strength_model 和 strength_clip，实现滤镜效果强度的无极调节。 | SDXL + LoRA + DepthAnythingV2 ControlNet           |

### A5: 终端交付层 (Terminal Delivery)

*商业交付的最后一道工序。本层绝对不包含任何扩散模型（Diffusion Model）的复杂去噪计算，全都是一次性的前向计算或矩阵操作，确保秒级出图与版权保护。*

| Lens ID         | 名称               | 核心功能                                                     | Inputs 资产                 | Outputs 资产 | Params (实际对应 JSON 节点参数)                              | 微调 UI 控件 (前端交互映射)                                  | 底层节点技术                                                 |
| --------------- | ------------------ | ------------------------------------------------------------ | --------------------------- | ------------ | ------------------------------------------------------------ | ------------------------------------------------------------ | ------------------------------------------------------------ |
| lens_watermark  | 水印与版权保护透镜 | 通过纯物理的图文排版矩阵，在最终高清图上动态叠加用户专属签名、Logo 或品牌防盗水印。 | BASE_IMAGE (前置透镜的成片) | RESULT_IMAGE | text (水印内容), font_name (字体文件), font_size (字号), font_color_hex (颜色), align, justify, margins (排版方位) | 沉浸式水印工坊 (Watermark Studio)：前端提供拖拽式文本框与调色板。用户在手机上实时输入文字、改变颜色和排版位置（左下/居中/全屏平铺等）。后端将排版数据翻译为 ComfyUI 的对齐参数与十六进制颜色值。 | CR Overlay Text (Comfyroll Studio)                           |
| lens_upscale_4x | 4倍超清放大透镜    | 针对图像进行画质提升与细节重绘。采用分块重绘（Tiled）技术，结合 UltraSharp 放大算法，在物理放大分辨率（4倍）的同时补充真实细节，修复模糊和噪点。 | BASE_IMAGE                  | RESULT_IMAGE | upscale_by (放大倍数), denoise (重绘幅度/细节添加量), prompt (正负向提示词), cfg (引导系数), tile_width / tile_height (分块尺寸), seed (随机种子) | 细节丰富度滑块 (Detail Enhancement)：前端滑块调节底层 Ultimate SD Upscale 的 denoise 参数（通常限制在 0.15-0.35 之间，数值越高新增细节越多，但过高会导致画面改变）；文本引导框 (Detail Prompt)：允许用户输入关键词以引导放大的具体纹理方向。 | SD 1.5 + Ultimate SD Upscale + 4x-UltraSharp (Upscale Model) |